// Arrow functions
const greet = (name) => `Hello, ${name}!`;

// Destructuring
const { x, y } = point;

// Async/await
async function fetchData() {
  const result = await fetch(url);
  return result.json();
}
