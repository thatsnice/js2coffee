----
async function withoutAwait() {
  return 42;
}
----
# Cannot be represented in CoffeeScript
# JS: Returns Promise {<fulfilled>: 42}
# CS: Would return 42 directly (not wrapped in Promise)
# Workaround: Use backticks `return Promise.resolve(42)`
withoutAwait = ->
  42
